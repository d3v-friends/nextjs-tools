import extract from "./extract";
import nullableParser from "./nullable-parser";
import parser from "./parser";

const fnSearchParams = {
	extract,
	parser,
	nullableParser,
};

export default fnSearchParams;
